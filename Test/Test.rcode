Write "Enter string >> "
str replaceFrom >>$readline
Write "Character to replace >> "
str characterToReplace >>$readline
Write "Character to replace "
WriteStr {characterToReplace}
Write "with >> "
str replaceWith >>$readline
replace:repla;with:replaceWith;str:replaceFrom;
Write "Result: "
WriteStr {replaceFrom}
